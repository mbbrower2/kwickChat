name: kwickchat
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
    - torch
    - pytorch-ignite
    - "transformers>=4.30.0"
    - tensorboardX
    - tensorflow
    - "spacy<3.0"
    - sentence-transformers
    - scikit-learn
    - numpy